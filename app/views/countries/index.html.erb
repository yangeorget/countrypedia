<% content_for :head do %>
    <title>Countrypedia</title>
    <meta name="keywords" content="<%= t('countries.index.keywords') %>">
    <meta name="description" content="<%= t('countries.index.description') %>">
<% end %>
<ol class="breadcrumb">
  <li><%= link_to t(:home), countries_path %></li>
</ol>

<div class="row">
  <div class="col-md-12">
    <%= render partial: "list", locals: {} %>
  </div>
  <div class="col-md-12">
    <%= render partial: "map", locals: {} %>
  </div>
</div>

